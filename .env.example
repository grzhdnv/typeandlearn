# Required for the default DeepSeek-backed LLM features.
DEEPSEEK_API_KEY=

# Optional: used only when a Groq fallback model is configured.
GROQ_API_KEY=

# Optional: omit to use apps/backend/data/db.sqlite.
DATABASE_URL=
