{
  "name": "typeandlearn-workspace",
  "private": true,
  "scripts": {
    "bootstrap": "bash scripts/bootstrap.sh",
    "check": "bash scripts/check.sh",
    "check:python": "uv run python -m unittest discover -s tests/backend -p 'test_*.py' && uv run python -m compileall apps/backend",
    "check:frontend": "npm --prefix apps/frontend run check"
  }
}
