{
  "name": "typeandlearn-workspace",
  "version": "0.1.0",
  "description": "Development workspace for the TypeAndLearn language-learning app.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "scripts": {
    "bootstrap": "bash scripts/bootstrap.sh",
    "dev": "bash scripts/dev.sh",
    "dev:backend": "uv run --locked fastapi dev apps/backend/main.py --host 127.0.0.1",
    "dev:frontend": "npm --prefix apps/frontend run dev -- --host 127.0.0.1 --strictPort",
    "check": "bash scripts/check.sh",
    "check:lint": "uv run ruff check apps/backend tests",
    "check:python": "uv run pyright && uv run python -m unittest discover -s tests/backend -p 'test_*.py' && uv run python -m compileall apps/backend",
    "check:frontend": "npm --prefix apps/frontend run check",
    "test:frontend": "node --test 'tests/frontend/**/*.test.ts'",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
